{
  "name": "user-service",
  "private": true,
  "scripts": {
    "build": "mvn clean package -DskipTests",
    "docker": "docker build -t $IMAGE_NAME .",
    "deploy": "echo Deploying user-service"
  }
}
